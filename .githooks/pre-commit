#!/bin/sh
# Auto-increment patch version (x.y.Z) in package.json on every commit.
# Major (x) and minor (y) must be updated manually.
# Install: git config core.hooksPath .githooks
cd "$(git rev-parse --show-toplevel)" || exit 1
node scripts/increment-version.js || exit 1
git add package.json
